def add_number(a, b, c):
    """return the sum of a, b and c"""
    return sum([a, b, c])


# 1. extend add number to allow an arbitrary number of params
# 2. investigate an resulve security
# 3. make implem more performant
# 4. add docs for add numbres


def calc_difference(a, b):
    return a - b
